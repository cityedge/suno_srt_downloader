# Suno SRT Downloader v1.2.0 User Guide

## このツールについて

Sunoの曲ページから同期歌詞を取得し、字幕向けに補正したSRTをダウンロードします。公開版でユーザーが変更できるのは「開始補正」だけです。その他の補正値と異常トークン修復は固定です。完成字幕を無修正で保証するツールではなく、SRT Tap Timerなどで直すための初期SRTを作るツールです。

## 重要

> **曲の生成後、Suno上で歌詞を編集するとその都度、同期タイミングが再構成されます。タイミングの精度は基本的に再構成のたびに下がります。歌詞を一度も編集していない状態で実行するようにしてください。**

歌詞を元の内容へ戻しても、生成直後の同期タイミングには戻りません。「現在の歌詞が元と同じ」ではなく、「生成後に一度も編集していない」ことが条件です。

## インストール

1. `index.html`をGoogle ChromeまたはMicrosoft Edgeで開きます。
2. ブックマークバーを表示します。通常は`Ctrl+Shift+B`です。
3. `Suno → SRT`ボタンをブックマークバーへドラッグします。
4. ドラッグできない場合は、ページ下部の`URLをコピー`を使って手動登録します。

## 使い方

1. Sunoへログインします。
2. 歌詞を一度も編集していない対象曲の`/song/{id}`または`/edit/{id}`ページを開きます。
3. ブックマークバーの`Suno → SRT`をクリックします。
4. 小さなダイアログで開始補正を選択します。
5. `ダウンロード`を押します。API取得中はマウスカーソルが待機表示になります。
6. `曲名.srt`がダウンロードされます。
7. 音源とSRTをSRT Tap Timer等へ読み込み、必要箇所を修正します。

ダイアログは`×`、背景クリック、`Esc`で閉じられます。`Enter`でもダウンロードを実行できます。

## 開始補正

選択肢は次の6段階です。

- 0.0秒
- -0.1秒（初期値）
- -0.2秒
- -0.3秒
- -0.4秒
- -0.5秒

負の値ほど字幕を歌唱開始より早く表示します。`ダウンロード`を押したときの値をSunoページの`localStorage`へ保存し、次回のダイアログで復元します。保存領域が利用できない場合でも、その回のダウンロードは選択値で実行できます。

保存キー：`cityedge.sunoSrtDownloader.startOffset`

## 固定パラメータ

| 項目 | 固定値 | 目的 |
|---|---:|---|
| 終了余韻 | +1.50秒 | Sunoの早すぎる終了を字幕向けに延ばす |
| 最大延長 | 0.40秒 | 短い空白だけ次字幕開始まで接続する |
| 字幕間ギャップ | 0.00秒 | 前字幕終了と次字幕開始を同時刻にできるようにする |
| 最低表示時間 | 0.10秒 | 極端に短い行を最低限残す |
| 重なり防止 | 有効 | 原則として次字幕開始を超えないようにする |
| 指示行除外 | 有効 | 明らかな英語制作指示行を保守的に削除する |
| 異常間隔判定 | 3.0秒超 | 行内の最大トークン間隔を検出する |
| 修復後間隔 | 1.50秒 | 検出した異常間隔を固定値へ圧縮する |

## 異常トークン間隔の自動修復

Sunoの単語時刻では、一部の音節が前後のセクションへ誤接続され、1行が十数秒から数十秒へ伸びる場合があります。v1.1.0で導入した固定処理をv1.2.0でも変更せず使用します。

有効な単語トークンが2個以上ある行で、すべての隣接トークンを確認します。隣接する前トークンをA、後トークンをBとすると、次の大きい方をトークン間隔として扱います。

```text
B.start - A.start
B.end   - A.end
```

行内の最大間隔が3.0秒を超えた場合、その間隔を1.5秒へ圧縮します。

- メタタグ後の最初の実歌詞行では、終了側をアンカーとして開始時刻を後ろへ移します。
- それ以外の歌詞行では、開始側をアンカーとして終了時刻を前へ移します。

括弧で囲まれた明らかな制作指示行は、メタタグ後の最初の実歌詞行として数えません。修復後には、選択した開始補正と、固定の終了余韻、次行への短い延長、重なり防止が適用されます。

この判定は実測事例に基づく固定ヒューリスティックです。正しい歌唱時刻が元データに存在しない場合、修復後も数秒のずれが残ります。また、短すぎる表示や長すぎる表示になることがあります。完全自動修正ではなく、致命的な異常を小さくする処理として使用してください。

## 時刻の選択

各歌詞行に埋め込まれた単語時刻が十分に揃っていれば単語時刻を使います。利用状況や順序が悪い場合は行時刻を使用します。優先側が欠けている行では、もう一方へフォールバックします。

## 出力

曲情報APIから曲名を取得し、Windowsで使えない文字を置換して`曲名.srt`として保存します。曲名取得に失敗した場合はページタイトルまたは曲IDを使います。文字コードはUTF-8です。

## 指示行の除外

`(Mid-tempo goa trance beat)`のように、丸括弧内の複数の英語制作指示語が不自然に短い時間へ割り当てられた行を除外します。`(Ah)`や`(Oh)`のような歌唱表現は残す設計ですが、完全な判定はできません。

## 使用上の留意点

- 大きなトークン間隔異常は圧縮しますが、Sunoの同期データ自体に正しい時刻がない行は完全には直せません。
- 特にSunoが歌詞を追加・反復した場合、行全体が別位置へ接続された場合、行・単語の両方が誤っている場合は手動修正が必要です。
- 最低表示時間より重なり防止を優先するため、異常行では0.10秒未満になる場合があります。
- 非公式APIに依存しています。
- ブックマークレットはSunoページ上で現在のログインセッションを利用します。信頼できる配布元のコードだけを登録してください。

## エラーと対処

| 表示・状態 | 対処 |
|---|---|
| Sunoの曲ページで実行してください | `/song/...`または`/edit/...`ページを開く |
| Sunoへのログインが必要です | ログインしてページを再読み込みする |
| ダイアログを閉じた | 何もダウンロードせず終了する。再度ブックマークレットを実行する |
| Suno APIエラー | 再ログイン、再読み込み後に再実行する。仕様変更の可能性もある |
| 出力できる歌詞行がありません | 歌詞付き曲か確認する。指示行だけの場合は出力されない |
| クリックしても反応しない | 古いブックマークを削除し、現在の`index.html`から登録し直す |

## Privacy

The bookmarklet sends requests only to Suno's own API using the existing Suno session. Lyrics and SRT data are processed in the browser and are not sent to a server operated by this project. The selected start offset is stored locally in the Suno origin's browser storage.

## ライセンス

本プロジェクトの新規コード・改修部分はCopyright (c) 2026 cityedge、MIT Licenseです。派生元`Suno 行単位 SRT ブックマークレット`の部分はCopyright (c) 2026 kuwa2005、MIT Licenseです。`LICENSE`と`THIRD_PARTY_NOTICES.md`を参照してください。
